# 🎯 QUICK REFERENCE CARD

**Status**: ✅ BOTH SERVERS RUNNING  
**Print this out or bookmark it!**

---

## 🌐 OPEN YOUR APPS

| App | URL | What to Expect |
|-----|-----|---|
| **Frontend (React)** | http://localhost:3002 | Dashboard UI |
| **Backend (API)** | http://localhost:3000 | API responses |
| **Health Check** | http://localhost:3000/health | JSON status |

---

## 🧪 TEST IN 30 SECONDS

### Copy & Paste in Browser Console (F12)

**Test 1: Is Backend Alive?**
```javascript
fetch('http://localhost:3000/health').then(r=>r.json()).then(d=>console.log('✅',d.status))
```

**Test 2: Can You Login?**
```javascript
fetch('http://localhost:3000/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:'admin@test.com',password:'SecurePass123!'})}).then(r=>r.json()).then(d=>console.log('✅ Token:',d.token.slice(0,20)+'...'))
```

---

## 🔐 PASSWORD MUST HAVE

✅ Minimum 12 characters  
✅ At least 1 UPPERCASE (A-Z)  
✅ At least 1 lowercase (a-z)  
✅ At least 1 number (0-9)  
✅ At least 1 special char (@$!%*?&)  

**Example**: `SecurePass123!` ✅

---

## 📊 API ENDPOINTS

| Method | Endpoint | Purpose |
|--------|----------|---------|
| GET | /health | Check if backend is alive |
| POST | /auth/login | Login user, get JWT token |
| POST | /auth/register | Register new user |
| POST | /auth/refresh | Refresh expired token |
| GET | /records | List all records |
| POST | /records | Create new record |
| GET | /records/:id | Get specific record |
| POST | /records/:id/reveal | Reveal encrypted data |
| GET | /audit-logs | View audit logs |

---

## 🔧 IF SOMETHING BREAKS

### Frontend Not Loading?
```powershell
cd frontend
npm run dev
```

### Backend Not Responding?
```powershell
cd backend
node server.js
```

### Need the docs?
- Quick test → `QUICK_TEST.md`
- System overview → `LIVE_SYSTEM_STATUS.md`
- Full testing guide → `SERVERS_STARTED_FUNCTIONALITY_TEST.md`
- All docs → `DOCUMENTATION_INDEX.md`

---

## 🎯 WHAT WORKS RIGHT NOW

✅ User authentication (JWT tokens)  
✅ Password validation (strong requirements)  
✅ Email validation (format + length)  
✅ Account locking (5 failed attempts)  
✅ Audit logging (immutable records)  
✅ API endpoints (all functional)  
✅ Error handling (graceful)  
✅ Rate limiting (100 req/min)  

---

## 📱 TRY THIS FLOW

1. **Open Frontend** → http://localhost:3002
2. **Check Backend** → http://localhost:3000/health
3. **Test Auth** → Use test command above
4. **Create Record** → POST /records with token
5. **View Logs** → GET /audit-logs

---

## 🚀 YOU'RE READY!

Both servers running ✅  
Models loaded ✅  
Documentation done ✅  
Ready to test ✅  

**Now go test! 🎉**

---

**Servers**:  
Frontend: http://localhost:3002  
Backend: http://localhost:3000  

**Docs**: DOCUMENTATION_INDEX.md

**Questions?** Check any of the documentation files above!
