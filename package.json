{
  "name": "ciphervault-fullstack",
  "version": "1.0.0",
  "description": "CipherVault - Secure encrypted data platform",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev -- --host 0.0.0.0 --port 3001\"",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev -- --host 0.0.0.0 --port 3001"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
